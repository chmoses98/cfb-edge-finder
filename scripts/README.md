# scripts/

Operational scripts (ingestion runners, discovery sweeps, snapshot capture)
begin in Milestone B. Nothing is built here yet -- this directory exists so
those milestones have an obvious, agreed-on home rather than requiring a
restructure, per `docs/ROADMAP.md`.
