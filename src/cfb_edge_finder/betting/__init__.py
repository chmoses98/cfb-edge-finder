"""Recommendation/staking layer: qualification tiers, correlation/risk gates,
bet-up-to pricing, CLV and calibration tracking.

Not implemented in this foundation phase (see docs/ROADMAP.md Milestones
G-H). This module exists so the package layout is stable for downstream
imports and so those milestones have an obvious home rather than needing a
restructure.
"""
